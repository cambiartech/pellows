import { AdminSection } from "../section";

export default function AdminInventoryPage() {
  return (
    <AdminSection
      title="Inventory"
      lede="LIVE units, search quality, and what guests actually get back."
      body="Ranking and typo aliases are already in search. This is where geo, slow queries, and a bad result get fixed without opening the database."
    />
  );
}
